version: '3.7'
services:
  backend:
    image: '${IMAGE_DOMAIN-localhost}/${PROJECT_NAME-django_vue}-backend:${TAG-latest}'
  frontend:
    image: '${IMAGE_DOMAIN-localhost}/${PROJECT_NAME-django_vue}-frontend:${TAG-latest}'
  celeryworker:
    image: '${IMAGE_DOMAIN-localhost}/${PROJECT_NAME-django_vue}-worker:${TAG-latest}'
  celerybeat:
    image: '${IMAGE_DOMAIN-localhost}/${PROJECT_NAME-django_vue}-worker:${TAG-latest}'
