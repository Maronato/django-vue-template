version: "3.7"
services:
  backend:
    environment:
      - FRONTEND_URL=http://localhost
      - DEBUG=True
  frontend:
    environment:
      - API_URL=http://backend:8000
      - API_URL_BROWSER=http://api.localhost
  celery_worker:
    environment:
      - DEBUG=True
