version: "3.7"
services:
  backend:
    build:
      context: ../backend
      args:
        APP_ENV: prod
  frontend:
    build:
      context: ../frontend
      args:
        APP_ENV: prod
  celery_worker:
    build:
      context: ../backend
      dockerfile: worker.dockerfile
      args:
        APP_ENV: prod
